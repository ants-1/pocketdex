import { SafeAreaView, Text } from "react-native";

export default function QuizScreen() {
  return (
    <SafeAreaView className="flex-1 items-center justify-center bg-white">
      <Text className="text-red-500 text-3xl font-bold">Quiz</Text>
    </SafeAreaView>
  )
}